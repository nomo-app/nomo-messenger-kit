{
  "name": "nomo-messenger-kit",
  "version": "0.1.0",
  "description": "A package for interacting with the Decentralized Nomo Messenger",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "doPublish": "npm publish",
    "generate_docs": "git checkout main && git diff --exit-code && typedoc src/nomo_messenger_api.ts --plugin typedoc-plugin-markdown --out api-docs/ --exclude **.ts && rm api-docs/README.md"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/nomo-app/nomo-messenger-kit.git"
  },
  "keywords": [],
  "author": "dev2@nomo.app",
  "license": "ISC",
  "devDependencies": {
    "typedoc": "^0.25.1",
    "typedoc-plugin-markdown": "^3.16.0",
    "typescript": "^5.2.2"
  }
}
